{
  "name": "cortex-memory-mcp",
  "version": "1.3.0",
  "type": "module",
  "description": "MCP server for querying and writing the Cortex vault (search, get, related, write, reload)",
  "main": "build/index.js",
  "scripts": {
    "prebuild": "node scripts/sync-version.mjs",
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node build/index.js",
    "inspector": "npx @modelcontextprotocol/inspector node build/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
