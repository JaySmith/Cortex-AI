[project]
name = "cortex-ai"
version = "1.4.0"
description = "Persistent, tiered memory for AI coding agents"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "typer>=0.12",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.5",
    "mypy>=1.10",
    "pytest>=8.0",
    "pre-commit>=3.7",
    "types-PyYAML>=6.0",
]

[project.scripts]
cortex = "cortex.cli.main:main"

[tool.setuptools.packages.find]
include = ["cortex", "cortex.*"]

[tool.setuptools.package-data]
cortex = ["templates/**/*"]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = []

[tool.ruff.lint.per-file-ignores]
# Legacy self-bootstrapping scripts — import aliasing and post-bootstrap imports
# are intentional patterns for venv detection. Deprecated shims have unused sys.
"gen-portfolio.py" = ["E402", "F821", "E501"]
"distill.py" = ["F401"]
"cortex-import.py" = ["F401"]
"cortex-uninstall.py" = ["F401", "E501"]
"hive_client.py" = ["F401"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
check_untyped_defs = true
no_implicit_optional = true

[[tool.mypy.overrides]]
module = [
    "cortex.distiller.core",
    "cortex.cli.commands.import_agent",
    "cortex.cli.commands.uninstall",
    "cortex.hub.client",
]
ignore_errors = true
